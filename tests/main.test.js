const index = require('../index.js')

describe('middleware', () => {
    const testDemo = require('./middleware/demo_only.js')
    const logs = require('./middleware/logs.js')
    const query_filters = require('./bluejay/query_filters.js')
    const no_cache = require('./middleware/no_cache.js')
})

