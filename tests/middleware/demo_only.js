var demoOnly = require('../../src/middleware/demo_only.js')

describe('demo only', () => {
    beforeEach(() => {
    })
    it('should return a function', () => {
    })
    describe('ctx.request.url === /DEMOBLOCKPAGE', () => {
        beforeEach(() => {
        })
        describe('checkPass resolves', () => {
            beforeEach(() => {
            })
            it('should call ctx.redirect', () => {
            })
        })
        describe('checkPass rejects', () => {
        })
    })
})
